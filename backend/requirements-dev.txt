# Dev / CI tooling — not part of runtime deps.
# Pin to avoid surprise rule changes breaking CI.

ruff>=0.16.6,<1.0
bandit>=1.9.4
aiosqlite>=0.22.1
alembic>=1.20.0
pytest>=8.0,<9.0
pytest-cov>=5.0
pytest-asyncio>=0.23
pytest-html>=4.1