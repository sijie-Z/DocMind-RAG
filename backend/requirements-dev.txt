# Dev / CI tooling — not part of runtime deps.
# Pin to avoid surprise rule changes breaking CI.

ruff>=0.6,<1.0
bandit>=1.7
aiosqlite>=0.20.0
alembic>=1.13.1
pytest>=8.0,<9.0
pytest-cov>=5.0
pytest-asyncio>=0.23
pytest-html>=4.1